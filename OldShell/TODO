RUN EVERYTHING ON SH
- set and unset env. 
	1. dub
	2. make chages

- getline
	handle -1 properly.
	- return value of getline is -1 and buffer is NULL
		print error
		break
-execve
	execve("/bin/ls", av + 1, env);
- quit the shell, handles the status
	use the wait(&status) and return it to bash
- dont worry about execv failing
- if malloc fails in the child process exit failure in child

